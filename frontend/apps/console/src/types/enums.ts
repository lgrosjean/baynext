export enum KpiType {
    Revenue = "revenue",
    NonRevenue = "non_revenue",
}

export enum JobStatus {
    Pending = "pending",
    Running = "running",
    Completed = "completed",
    Failed = "failed",
}